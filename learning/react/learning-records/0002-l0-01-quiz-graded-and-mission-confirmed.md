# L0-01 自测批改:投影方向误解;动因与路线确认

**自测结果(2026-08-19,纯文本作答)**:4 题对 2(声明式 ✓、重渲染 ✓)。两个错答都是高价值信号:

1. **「界面是谁的投影」答了 dom(应为状态)——方向反了**:把「被写的介质」当成了「源头」。这是学前端的旧直觉(DOM 是主角)的自然残留。已在批改回复中用「DOM ≈ 物化视图的存储,状态才是基础表」+ 失效模式「手改 React 管的 DOM,下次重渲染就被覆盖」重新锚定,并预告了这个误解如果不修正将在 L1 变成「手改 DOM 不生效」「状态和界面对不上」类困惑。**待 L0-02 自测和 L1-01 再次回收验证是否扎根。**
2. **「目标线」答了专家(应为职业线)**——按误答处理,但已在回复中显式追问一次;若确认为真实意愿(想深入渲染原理/构建链),需修订 MISSION 的 out-of-scope。

**Mission 参数确认(已同步修订 MISSION/NOTES/ROADMAP)**:动因 = 工作全栈交付 + 个人学习;排课 = 按内容不按时间(自测通过 + 动手完成触发下一课);路线 = 混合。L4 选题方向:优先工作真实全栈功能。

## Evidence
学习者一回合纯文本提交全部答案与三个确认项,无提示,节奏干脆——自测参与度高,但两处概念未扎根,符合「fluency 高于 storage」的典型形态。
